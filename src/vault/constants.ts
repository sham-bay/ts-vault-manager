/**
 * Default values used across the package.
 * Consumers can override most of these via per-call options.
 */

/** 5 minutes */
export const DEFAULT_REFRESH_BUFFER_SECONDS = 300;

/** 55 minutes */
export const DEFAULT_FALLBACK_TTL_SECONDS = 3300;
